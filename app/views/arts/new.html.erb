<div class="forms">
  <div class="container">
    <div class="row">
      <div class="col-sm-8 col-sm-offset-2">
        <div class="form-upload-art">
          <% if current_user.is_a?(StreetArtist) %>
            <h2 class="text-center">Upload Your Art</h2>
          <% else %>
            <h2 class="text-center">Upload Art</h2>
          <% end %>
          <%= render "form" %>
        </div>
      </div>
    </div>
  </div>
</div>

